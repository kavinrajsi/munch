<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#1d5d31">
    {% if settings.favicon != blank %}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {% endif %}
    <title>
      {{ page_title }}
      {% if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif %}
      {% if current_page != 1 %} &ndash; Page {{ current_page }}{% endif %}
      {% unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless %}
    </title>
    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}
    {% comment %} {% render 'social-meta-tags' %} {% endcomment %}
    <link rel="canonical" href="{{ canonical_url }}">
    <link rel="dns-prefetch" href="https://cdn.shopify.com">
    <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
    <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>

    {{ 'slick.css' | asset_url | preload_tag: as: 'style' }}
    {{ 'slick-theme.css' | asset_url | preload_tag: as: 'style' }}
    {{ 'base.css' | asset_url | preload_tag: as: 'style' }}

    {{ 'jquery.js' | asset_url | preload_tag: as: 'script' }}
    {{ 'shopify-money.js' | asset_url | preload_tag: as: 'script' }}
    {{ 'predictive-search.js' | asset_url | preload_tag: as: 'script' }}
    {{ 'global.js' | asset_url | preload_tag: as: 'script' }}
    {{ 'drawer.js' | asset_url | preload_tag: as: 'script' }}
    {{ 'drawer-product.js' | asset_url | preload_tag: as: 'script' }}
    {{ 'slick.min.js' | asset_url | preload_tag: as: 'script' }}
    {{ 'photoswipe-control.js' | asset_url | preload_tag: as: 'script' }}

    {{ content_for_header }}

    {% style %}
      {{ settings.type_header | font_face }}
      {{ settings.type_body | font_face }}
      @font-face {
        font-family: 'minion_pro_cond_captregular';
        src: url('{{ "minion_pro_cond_caption-webfont.woff2" | asset_url }}') format('woff2'),
              url('{{ "minion_pro_cond_caption-webfont.woff" | asset_url }}') format('woff');
        font-weight: normal;
        font-style: normal;
        font-display: swap;
    }
    :root {
      --font-minion:  'minion_pro_cond_captregular';
      --font-header: {{settings.type_header.family}};
      --font-body:  {{settings.type_body.family}};
    }
    {% endstyle %}

    <link rel="stylesheet" href="{{'slick.css' | asset_url }}">
    <link rel="stylesheet" href="{{'slick-theme.css' | asset_url }}">
    <link rel="stylesheet" href="{{'base.css' | asset_url }}">
  </head>

  <body class="template-{{ template | split : '.' | first }}  {{ page_title | handleize }}">
    <!--[if IE]>
      <p class="browserupgrade">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
    <![endif]-->
    {% section 'announcement-bar' %}
    {% section 'header' %}
    <main class="layout__main-content">
      {{ content_for_layout }}
    </main>
    {% section 'footer' %}
    {% section 'drawer' %}
    {% section 'drawer-product' %}
    {% section 'predictive-search' %}
    {% assign potential_action_target = request.origin
      | append: routes.search_url
      | append: '?q={search_term_string}'
    %}
    <script type="application/ld+json">
      {
        "@context": "http://schema.org",
        "@type": "WebSite",
        "name": {{ shop.name | json }},
        "potentialAction": {
          "@type": "SearchAction",
          "target": {{ potential_action_target | json }},
          "query-input": "required name=search_term_string"
        },
        "url": {{ request.origin | append: page.url | json }}
      }
    </script>
    <script type="application/ld+json">
      {
        "@context": "http://schema.org",
        "@type": "Organization",
        "name": {{ shop.name | json }},
        {% if settings.brand_logo != blank %}
        "logo": "https://cdn.shopify.com/s/files/1/0490/1158/9282/files/sundari-silks-logo.png",
        {% endif %}
        "sameAs": [
          {{ settings.social_twitter_link | json }},
          {{ settings.social_facebook_link | json }},
          {{ settings.social_pinterest_link | json }},
          {{ settings.social_instagram_link | json }},
          {{ settings.social_youtube_link | json }}
        ],
        "url": {{ request.origin | append: page.url | json }}
      }
    </script>

    <script defer src="{{ 'jquery.js' | asset_url }}"></script>
    <script defer src="{{ 'shopify-money.js' | asset_url }}"></script>
    <script defer src="{{ 'predictive-search.js' | asset_url }}"></script>
    <script defer src="{{ 'drawer.js' | asset_url }}"></script>
    <script defer src="{{ 'drawer-product.js' | asset_url }}"></script>
    <script defer src="{{ 'slick.min.js' | asset_url }}"></script>
    <script defer src="{{ 'global.js' | asset_url }}"></script>

    {% if template contains 'product' %}
      <script defer src="{{ 'photoswipe-control.js' | asset_url }}"></script>
    {% endif %}

    <script>
      document.addEventListener('DOMContentLoaded', function () {
        const productHandle = '{{ product.handle }}';
        let viewed = JSON.parse(localStorage.getItem('recently_viewed')) || [];

        // Remove current handle if it exists already
        viewed = viewed.filter((h) => h !== productHandle);

        // Add current product to beginning
        viewed.unshift(productHandle);

        // Limit to 5 items
        viewed = viewed.slice(0, 5);

        localStorage.setItem('recently_viewed', JSON.stringify(viewed));
      });
    </script>
  </body>
</html>
